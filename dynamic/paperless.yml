http:
  routers:
    paperless:
      entryPoints:
        - http
      service: paperless
      rule: Host(`paperless.{{env "DOMAINNAME"}}`)
  services:
    paperless:
      loadBalancer:
        servers:
          - url: "http://paperless:8000/"
